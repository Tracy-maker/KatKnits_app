import React from "react";

interface StabBlockProps {
  value: string | number;
  label: string;
}

const StackBlock = ({ value, label }: StabBlockProps) => {
  return <div>StackBlock</div>;
};

export default StackBlock;
